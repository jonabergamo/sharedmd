PORT=3000
# leave empty to run without redis (in-memory rooms, single instance)
REDIS_URL=
# only needed when the web app is served from another origin (vite dev proxy makes this unnecessary)
ORIGIN=
